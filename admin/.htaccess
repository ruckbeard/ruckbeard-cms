AddType application/x-httpd-php .html

<IfModule mod_rewrite.c>
	RewriteEngine On
	RewriteBase /ruckbeard/admin

	RewriteCond %{REQUEST_FILENAME} !-d
	RewriteCond %{REQUEST_FILENAME} !-f
	RewriteCond %{REQUEST_FILENAME} !-l
	
</IfModule>